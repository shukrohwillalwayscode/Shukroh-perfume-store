export interface PerfumeTypes {
  title: string;
  description: string;
  img: string;
  price: string;
}
export interface ReviewData {
  id: number;
  rating: string;
  review: string;
  name: string;
}

export interface Button {
  title: string;
  bgcolor: string;
  borderColor: string;
  color: string;
}
